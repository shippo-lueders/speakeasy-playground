---
openapi: 3.0.0
info:
  title: Example
  version: '1'
  description: ''
servers:
- url: https://example.com
paths:
  "/example":
    get:
      operationId: Example
      parameters:
        - $ref: "#/components/parameters/HeaderParam"
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ExampleBody'
      responses:
        '200':
          description: example
components:
  parameters:
    HeaderParam:
      description: The number of results to return per page (max 100)
      in: header
      name: header_param
      schema:
        type: string
  responses: {}
  schemas:
    ExampleBody:
      type: object
      properties:
        enum_field:
          type: string
          enum:
            - value1
            - value2
        optional_enum_field:
          oneOf:
            - type: string
            - type: string
              enum:
                - value3
                - value4
              x-speakeasy-name-override: optional_enum_field
        other_optional_enum_field:
          oneOf:
            - type: string
            - type: string
              enum:
                - value5
                - value6
              x-speakeasy-name-override: other_optional_enum_field
        status:
          $ref: "#/components/schemas/Status"
    Status:
      oneOf:
        - type: string
        - $ref: "#/components/schemas/StatusEnum"
    StatusEnum:
      type: string
      enum:
        - VALID
        - INVALID
